<div align="center">

# ابزارک — مجموعه ابزارهای آفلاین فارسی

مجموعه‌ای از ابزارهای سریع، کاربردی و کاملاً آفلاین برای تولید نام، نام کاربری، رمز عبور، ایمیل آزمایشی، نام برند، کد تصادفی و UUID.

بدون سرور. بدون API. بدون ردیابی. همه‌چیز فقط در مرورگر شما اجرا می‌شود.

[![HTML5](https://img.shields.io/badge/HTML5-E34F26?style=flat-square&logo=html5&logoColor=white)](#)
[![CSS3](https://img.shields.io/badge/CSS3-1572B6?style=flat-square&logo=css3&logoColor=white)](#)
[![JavaScript](https://img.shields.io/badge/JavaScript-F7DF1E?style=flat-square&logo=javascript&logoColor=black)](#)
[![PWA](https://img.shields.io/badge/PWA-Ready-5A0FC8?style=flat-square&logo=pwa&logoColor=white)](#)
[![Offline First](https://img.shields.io/badge/Offline-First-2EA44F?style=flat-square)](#)
[![No Backend](https://img.shields.io/badge/Backend-None-lightgrey?style=flat-square)](#)
[![License](https://img.shields.io/badge/License-Not%20Specified-yellow?style=flat-square)](#-license)

</div>

---

## 📸 پیش‌نمایش

![Dashboard](docs/screenshots/dashboard.png)

![Dark Mode](docs/screenshots/dark-mode.png)

![Dark Mode](docs/screenshots/password-generator.png)

![Dashboard](docs/screenshots/mobile.png)


---

## 🚀 دمو

این پروژه یک اپلیکیشن فرانت‌اند استاتیک است و می‌تواند به‌راحتی روی **GitHub Pages** میزبانی شود.

🔗 دموی زنده: `https://abolfazldev2022.github.io/Abzarak-toolkit/`


---

## 🧭 دربارهٔ پروژه

**ابزارک** یک جعبه‌ابزار وب فارسی و راست‌به‌چپ (RTL) است که چند تولیدکنندهٔ تصادفی پرکاربرد را در یک رابط واحد، سریع و مدرن جمع کرده — بدون این‌که هیچ داده‌ای از دستگاه شما خارج شود.

### چه مشکلی را حل می‌کند؟

معمولاً برای تولید یک اسم مستعار، نام کاربری، رمز عبور امن یا یک UUID، یا باید به چند سایت مختلف سر زد، یا از ابزارهایی استفاده کرد که داده‌های شما (از جمله رمز عبور!) را به یک سرور ارسال می‌کنند. ابزارک همهٔ این ابزارها را در یک صفحهٔ فارسی، سبک و **۱۰۰٪ آفلاین** کنار هم قرار می‌دهد.

---

## ✨ امکانات

### 🎲 تولید نام تصادفی فارسی
- نام‌های خلاقانه، خنده‌دار اما شیک، مناسب نام مستعار
- ترکیب از پایگاه واژگان دسته‌بندی‌شده (حیوانات، خوراکی، طبیعت، فناوری، فضا، اشیا و صفت‌ها)
- تولید با یک کلیک
- دکمهٔ کپی
- ثبت در تاریخچهٔ محلی

### 👤 تولید نام کاربری
- زبان: فارسی / English / ترکیبی
- طول قابل تنظیم
- افزودن اعداد، آندرلاین (`_`)، نقطه (`.`)، خط تیره (`-`)
- حروف بزرگ تصادفی

### 🔐 تولید رمز عبور
- طول از ۸ تا ۱۲۸ کاراکتر
- حروف بزرگ، حروف کوچک، اعداد، نمادها
- نمایشگر قدرت رمز (ضعیف تا بسیار قوی)
- نمایش/مخفی‌کردن رمز
- کپی با یک کلیک
- تصادفی‌سازی رمزنگاری‌شده با `crypto.getRandomValues()`

### 📧 تولید ایمیل آزمایشی
> این ابزار **ایمیل واقعی نمی‌سازد**. فقط رشته‌هایی شبیه ایمیل، برای تست و نمونه‌سازی (روی دامنه‌های ساختگی مانند `example.com`) تولید می‌کند و به هیچ صندوق پستی واقعی متصل نیست.

### 🏷️ تولید نام برند / پروژه
دسته‌بندی‌ها: برند، پروژهٔ نرم‌افزاری، اپلیکیشن، فروشگاه، بازی، هوش مصنوعی، استارتاپ، تکنولوژی

### 🔑 تولید کد تصادفی
- طول دلخواه
- اعداد، حروف بزرگ، حروف کوچک، نمادها
- جداکننده‌های قابل انتخاب
- قالب‌های آماده (۴ رقمی، ۶ رقمی، OTP، کلید لایسنس و…)

### 🆔 تولید UUID
- UUID نسخهٔ ۴
- تولید چندتایی (۱ تا ۲۰ عدد)
- پشتیبانی از کپی

### 📋 تاریخچهٔ محلی
- آخرین موارد تولیدشده
- کپی و حذف تکی
- پاک‌سازی کامل
- ذخیره‌سازی فقط در `localStorage`
- **رمزهای عبور به‌صورت پیش‌فرض در تاریخچه ذخیره نمی‌شوند**

### 🌙 پوسته
- روشن / تاریک / سیستم
- ذخیرهٔ پایدار انتخاب پوسته

### 📱 واکنش‌گرا
- موبایل، تبلت و دسکتاپ

### 📦 PWA
- قابل نصب
- Offline-first
- Service Worker
- تمام asset ها محلی

### 🔒 حریم خصوصی
- بدون بک‌اند
- بدون API
- بدون ردیابی و تحلیل‌گر
- داده‌های تولیدشده روی دستگاه شما باقی می‌مانند
- رمزهای عبور به‌صورت پیش‌فرض ذخیره نمی‌شوند

---

## 🛠️ فناوری‌های استفاده‌شده

| فناوری | کاربرد |
|---|---|
| HTML5 | ساختار صفحه |
| CSS3 | استایل‌دهی و طراحی واکنش‌گرا |
| Vanilla JavaScript | منطق اپلیکیشن (بدون فریم‌ورک) |
| LocalStorage | ذخیرهٔ تنظیمات و تاریخچه به‌صورت محلی |
| Web Crypto API | تولید تصادفی امن (`crypto.getRandomValues`) |
| Service Worker | قابلیت آفلاین و PWA |
| Web App Manifest | نصب‌پذیری اپلیکیشن |

---

## 📂 ساختار پروژه

```
.
├── index.html              صفحهٔ اصلی (تک‌صفحه‌ای، شامل همهٔ بخش‌ها)
├── manifest.json            فایل مانیفست PWA
├── sw.js                     Service Worker برای کش آفلاین
├── README.md
├── css/
│   ├── style.css             متغیرهای طراحی، پایه، چیدمان و کامپوننت‌ها
│   └── responsive.css         نقاط شکست واکنش‌گرا
├── js/
│   ├── app.js                  اتصال ناوبری، صفحات و رویدادها
│   ├── generators.js            منطق تولید تصادفی + پایگاه واژگان
│   ├── storage.js                لایهٔ ایمن روی localStorage
│   └── ui.js                      توست، مودال، پوسته، کلیپ‌بورد
└── assets/
    ├── fonts/                 محل فایل‌های فونت محلی Vazirmatn (خالی — پایین را ببینید)
    └── icons/
        └── icon.svg            آیکون برنامه / PWA
```

> پوشهٔ `assets/fonts/` در حال حاضر خالی است؛ برنامه بدون آن هم با فونت‌های جایگزین سیستم به‌درستی کار می‌کند (به بخش «شخصی‌سازی» مراجعه کنید).

---

## ▶️ نصب و اجرا

### کلون کردن ریپازیتوری

```bash
git clone https://github.com/YOUR-USERNAME/YOUR-REPOSITORY.git
cd YOUR-REPOSITORY
```

### روش سریع (بدون سرور)

فایل `index.html` را مستقیماً در مرورگر باز کنید. تمام تولیدکننده‌ها، تاریخچه، تنظیمات و پوسته‌ها به‌طور کامل کار می‌کنند.

### روش کامل (با Service Worker / PWA)

مرورگرها Service Worker را روی `file://` اجرا نمی‌کنند؛ برای فعال شدن کامل PWA و کش آفلاین باید پروژه از طریق `http(s)` یا `localhost` سرو شود:

```bash
python -m http.server 8000
```

سپس در مرورگر باز کنید:

```
http://localhost:8000
```

---

## 🌐 استقرار روی GitHub Pages

1. ریپازیتوری را در GitHub باز کنید.
2. به تب **Settings** بروید.
3. از منوی کناری، **Pages** را انتخاب کنید.
4. در بخش **Build and deployment**، گزینهٔ **Deploy from a branch** را انتخاب کنید.
5. شاخهٔ **main** را انتخاب کنید.
6. پوشهٔ **/root** را انتخاب کنید.
7. روی **Save** کلیک کنید.

پس از چند دقیقه، آدرس نهایی سایت شما در همان صفحهٔ Pages نمایش داده می‌شود (معمولاً به شکل `https://YOUR-USERNAME.github.io/YOUR-REPOSITORY/`).

همچنین می‌توانید از همان بخش Pages، یک **دامنهٔ اختصاصی (Custom domain)** متصل کنید.

---

## 🧱 معماری آفلاین

ابزارک به‌طور کامل به‌صورت **Static Site** ساخته شده:

- HTML، CSS و JavaScript همگی فایل‌های ثابت و محلی هستند.
- هیچ فراخوانی به API خارجی، CDN یا فونت آنلاین در کد وجود ندارد.
- تصادفی‌سازی امن (مثلاً برای رمز عبور و UUID) با **Web Crypto API** و در همان مرورگر انجام می‌شود.
- تنظیمات، تاریخچه و علاقه‌مندی‌ها فقط در **LocalStorage** همان دستگاه ذخیره می‌شوند.
- **Service Worker** (`sw.js`) در صورت سرو شدن از HTTPS/localhost، فایل‌های اپلیکیشن را کش می‌کند تا بازدیدهای بعدی بدون اینترنت هم کار کنند.

هیچ رمز عبور، نام کاربری یا داده‌ای که تولید می‌کنید، به هیچ سروری ارسال نمی‌شود.

---

## 🔒 حریم خصوصی و امنیت

- بدون سرور، بدون دیتابیس، بدون API
- بدون تحلیل‌گر (Analytics) و بدون ردیاب (Tracking)
- تولید رمز عبور به‌طور کامل و محلی در مرورگر انجام می‌شود
- برای تولید مقادیر تصادفی امن (مانند رمز عبور و UUID) از **Web Crypto API** استفاده می‌شود
- ذخیرهٔ رمزهای عبور در تاریخچه به‌صورت پیش‌فرض **غیرفعال** است
- LocalStorage فقط برای تنظیمات و تاریخچهٔ محلی استفاده می‌شود
- کنترل کامل داده‌های ذخیره‌شده (پاک کردن تاریخچه / بازنشانی تنظیمات / پاک کردن همهٔ داده‌ها) در اختیار کاربر است

> این پروژه ادعای گواهی امنیتی یا ممیزی رسمی ندارد؛ توضیحات بالا صرفاً معماری فنی پروژه را شفاف بیان می‌کند.

---

## 🎨 شخصی‌سازی

| می‌خواهید چه چیزی را تغییر دهید؟ | کجا؟ |
|---|---|
| افزودن واژهٔ فارسی جدید به تولیدکنندهٔ نام | شیء `WORDS` در `js/generators.js` |
| افزودن دسته‌بندی جدید (مثلاً یک دستهٔ تازه از اسم/برند) | `js/generators.js` — آرایه‌های `WORDS` و `BRAND_SYLLABLES_*` |
| تغییر رنگ‌ها (روشن/تاریک) | متغیرهای CSS در ابتدای `css/style.css` (`:root` و `[data-theme='dark']`) |
| تغییر فونت | بخش `@font-face` در `css/style.css` + قرار دادن فایل‌های woff2 در `assets/fonts/` |
| تغییر نمادهای مجاز رمز عبور | شیء `CHAR_POOLS.symbols` در `js/generators.js` |
| تغییر اندازهٔ پیش‌فرض تاریخچه | `DEFAULT_SETTINGS.historySize` در `js/storage.js`، یا از صفحهٔ تنظیمات در برنامه |

---

## 🌍 سازگاری با مرورگرها

Chrome، Firefox، Edge، Safari (دسکتاپ و موبایل) و Android Chrome. تصادفی‌سازی امن با `crypto.getRandomValues` انجام می‌شود که در همهٔ مرورگرهای مدرن پشتیبانی می‌شود؛ در نبود آن، برنامه به‌طور خودکار روی `Math.random` سوییچ می‌کند تا از کار نیفتد.

---

## 🤝 مشارکت (Contributing)

مشارکت شما خوش‌آمد است:

1. ریپازیتوری را **Fork** کنید.
2. یک شاخهٔ جدید بسازید: `git checkout -b feature/my-feature`
3. تغییرات خود را اعمال کنید.
4. اپلیکیشن را در مرورگر تست کنید (صفحات، پوسته‌ها، اندازه‌های مختلف صفحه).
5. یک **Pull Request** باز کنید و تغییرات را توضیح دهید.

---

## 🗺️ نقشهٔ راه (Roadmap)

- [ ] افزودن دسته‌بندی‌های بیشتر برای تولید نام فارسی
- [ ] گزینه‌های شخصی‌سازی بیشتر
- [ ] بهبود سیستم علاقه‌مندی‌ها (Favorites)
- [ ] Export / Import تنظیمات
- [ ] افزودن ابزارهای کاربردی بیشتر
- [ ] بهبود بیشتر تجربهٔ موبایل

---

## 📄 License

برای این پروژه هنوز لایسنس مشخص نشده است.

می‌توانید بعداً یک فایل `LICENSE` (مثلاً MIT) به ریشهٔ ریپازیتوری اضافه کرده و این بخش را با نوع لایسنس واقعی به‌روزرسانی کنید.

---

## ❓ سوالات متداول (FAQ)

**آیا برای استفاده از ابزارک به اینترنت نیاز است؟**
خیر. بخش اصلی اپلیکیشن به‌طور کامل آفلاین کار می‌کند.

**آیا رمزهای عبور تولیدشده به سروری ارسال می‌شوند؟**
خیر. تمام تولید رمز به‌صورت محلی، در همان مرورگر شما انجام می‌شود.

**آیا می‌توانم پروژه را روی GitHub Pages قرار دهم؟**
بله. این پروژه یک اپلیکیشن استاتیک است و برای GitHub Pages مناسب است.

**آیا می‌توانم دامنهٔ اختصاصی متصل کنم؟**
بله، از طریق تنظیمات GitHub Pages.

**آیا تولیدکنندهٔ ایمیل، ایمیل واقعی می‌سازد؟**
خیر. فقط رشته‌هایی شبیه ایمیل برای تست و نمونه‌سازی تولید می‌کند.

**آیا این پروژه PWA است؟**
بله، هنگامی‌که از یک محیط پشتیبانی‌شده مانند HTTPS یا `localhost` سرو شود.

---

<div align="center">

ساخته‌شده با ❤️ و بدون هیچ وابستگی به سرور.

</div>
